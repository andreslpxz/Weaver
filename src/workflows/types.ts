/**
 * FASE 3 — Punto de entrada de tipos de workflows.
 *
 * Mantiene compatibilidad con imports existentes `from '@/workflows/types'`.
 * Los tipos viven en `./types/` (subcarpeta especializada).
 */

export * from './types/index';
