name: Build Windows

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install npm dependencies
        run: npm ci

      - name: Build Tauri app (Windows)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'Weaver ${{ github.ref_name }}'
          releaseBody: 'See the assets below to download this version.'
          releaseDraft: true
          prerelease: false
          args: --target x86_64-pc-windows-msvc

      - name: Upload .msi + .exe artifacts
        uses: actions/upload-artifact@v4
        with:
          name: weaver-windows
          path: |
            src-tauri/target/x86_64-pc-windows-msvc/release/bundle/msi/*.msi
            src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
          if-no-files-found: warn

  # Code signing opcional — requiere secrets con certificado EV
  # Descomentar cuando se tenga el certificado:
  #
  # sign:
  #   needs: build
  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/download-artifact@v4
  #       with:
  #         name: weaver-windows
  #     - name: Sign with signtool
  #       env:
  #         CERT_PFX: ${{ secrets.WINDOWS_CERT_PFX }}
  #         CERT_PASS: ${{ secrets.WINDOWS_CERT_PASS }}
  #       run: |
  #         echo $env:CERT_PFX | base64 -d > cert.pfx
  #         signtool sign /f cert.pfx /p $env:CERT_PASS \
  #           /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 \
  #           *.exe *.msi
